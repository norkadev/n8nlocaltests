# PostgreSQL Database Configuration
# These variables are used by the postgres service in docker-compose.yml
# and the init-data.sh script for database initialization

# Root PostgreSQL user credentials (used by postgres container)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=n8n

# Non-root PostgreSQL user credentials (created by init-data.sh)
# This user will be used by n8n to connect to the database
POSTGRES_NON_ROOT_USER=n8n_user
POSTGRES_NON_ROOT_PASSWORD=n8n_user